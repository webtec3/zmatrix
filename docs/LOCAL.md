# ZMatrix PHP Extension

ZMatrix é uma extensão nativa para PHP escrita em C++ que fornece suporte a operações de álgebra linear com foco em performance.

## 🚀 Instalação

```bash
phpize
./configure --enable-zmatrix
make
sudo make install
